<?php
/**
 * IT-Kayali Translate intentionally preserves translation data on uninstall in V0.1.
 * A future uninstall screen will offer explicit cleanup choices.
 */
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}
